apiVersion: 1

datasources:
- name: InfluxDB
  type: influxdb
  access: proxy
  url: http://influxdb:8086
  isDefault: true
  editable: true
  password: $INFLUXDB_USER_PWD
  user: $INFLUXDB_USER_NAME
  database: $INFLUXDB_NAME